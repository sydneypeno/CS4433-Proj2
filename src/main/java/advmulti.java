// An advanced multi-iteration K-means algorithm that terminates
// potentially earlier if it converges based on some threshold in
// similarity distance (or for safeguard after R iterations say R=20).

public class advmulti {

    // same as multi but convergence check after each iteration
    // after each iteraion, calulate the distance change of centroids
    //if the change is below threshold or after R iterations, end

}
