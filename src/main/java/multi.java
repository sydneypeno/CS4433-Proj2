//basic multi-iteration Kmeans algorithm that terminates after R iterations
// (remember to set the parameter R to terminate the process, e.g., R=6)

public class multi {

    //Multiple MR jobs (1 for each iteration)
    // Mapper and Reducer will be the same as single but iterate R times then stop


}
